

#include "csHeaderInfo.h"

using namespace cseis_geolib;

/*
bool csHeaderInfo::operator==( const csHeaderInfo& obj ) const {
  return !name.compare( obj.name );
  //bool retFalse =
  //  (name.compare( obj.name )) ||
  //  (type != obj.type) ||
  //  (description.compare( obj.description ));
  //return !retFalse;
}
bool csHeaderInfo::operator!=( const csHeaderInfo& obj ) const {
  return name.compare( obj.name );
  //bool retTrue =
  //  (name.compare( obj.name )) ||
  //  (type == obj.type) ||
  //  (description.compare( obj.description ));
  //return retTrue;
}
bool csHeaderInfo::operator>( const csHeaderInfo& obj ) const {
  return( name > obj.name );
}
bool csHeaderInfo::operator<( const csHeaderInfo& obj ) const {
  return( name < obj.name );
}
bool csHeaderInfo::operator<=( const csHeaderInfo& obj ) const {
  return( name <= obj.name );
}
bool csHeaderInfo::operator>=( const csHeaderInfo& obj ) const {
  return( name >= obj.name );
}
*/


